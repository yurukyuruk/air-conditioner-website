import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://yurukyuruk.github.io',
  base: 'air-conditioner-website',
  output: 'static',
});
