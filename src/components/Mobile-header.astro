---
import Logo from "./Logo.astro";
---

<script>
    const hamburgerButton = document.querySelector(".hamburger-menu-icon");
    const navigation = document.querySelector(".navigation");
    hamburgerButton.addEventListener("click", () => {
        navigation.classList.toggle("hidden");
    })

    let lastScrollTop = 0;
    window.addEventListener("scroll", function() {
        const currentScroll = window.scrollY || document.documentElement.scrollTop;
        if (currentScroll > lastScrollTop) {
            // Scroll down
            document.querySelector(".header").classList.add("header-hidden");
            document.querySelector(".navigation").classList.add("scroll-hidden");
        } else {
            // Scroll up
            document.querySelector(".header").classList.remove("header-hidden");
            document.querySelector(".navigation").classList.remove("scroll-hidden");
        }
        lastScrollTop = currentScroll <= 0 ? 0 : currentScroll; // For Mobile or negative scrolling
    }, false);

    document.addEventListener("DOMContentLoaded", function() {
    const currentPage = window.location.pathname; // Get current page pathname
    // Get all subpage links
    const links = document.querySelectorAll("nav ul li a");
    // Loop through links
    links.forEach(function(link) {
        if (link.getAttribute("href") === currentPage) {
            link.classList.add("active"); // Add active class to current page link
        }
    });
});
</script>

<section class="header">
    <div class="header-content">
        <a class="logo-part" href="/">
            <Logo></Logo>
            <h2><span class="m-letter">M</span><span class="s-letter">S</span> INSTALBUD</h2>
        </a>
        <button class="hamburger-menu-icon"><img src="../../public/images/hamburger-menu.png"></button>
    </div>
    <nav class="navigation hidden">
        <ul>
            <li><a href="/o-nas">O nas</a></li>
            <li><a href="/kontakt">Kontakt</a></li>
            <li><a href="/realizacje">Realizacje</a></li>
            <li><a href="/serwis">Serwis</a></li>
            <li><a href="/certifikaty">Certifikaty</a></li>
            <li><a href="/klimatyzacje">Klimatyzacje</a></li>
            <li><a href="/wentylacja">Wentylacja</a></li>
            <li><a href="/pompy-ciepla">Pompy Ciepła</a></li>
        </ul>
    </nav>
</section>

<style>
    .header {
        display: flex;
        flex-direction: column;
        box-shadow: rgba(0, 0, 0, 0.16) 0px 1px 4px;
        align-items: center;
        position: fixed;
        top: 0;
        transition: top 0.3s;
        width: 100%;
    }
    .header-hidden {
        top: -74px; 
    }
    .scroll-hidden {
        display: none;
    }
    .header-content {
        padding-top: 10px;
        padding-bottom: 10px;
        display: flex;
        justify-content: space-between;
        width: var(--width-for-mobile);
        font-family: var(--root-font-family);
    }
    .logo-part {
        display: flex;
        align-items: center;
        padding-left: 0;
    }
    h2 {
        font-weight: var(--light-font-weight);
        margin: 0;
        align-self: end;
        color: var(--main-blue-color);
        padding-left: 6px;
    }
    .m-letter {
        color: var(--main-blue-color);
    }
    .s-letter {
        color: var(--main-red-color);
    }
    a {
        text-decoration: none;
        font-family: var(--root-font-family);
        color: var(--main-blue-color);
        cursor: pointer;
        width: var(--width-for-mobile);
    }
    a:active, a:hover {
        color: black;
    }
    .hamburger-menu-icon img{
        width: 30px;
        height: 30px;
        cursor: pointer;
    }
    .hamburger-menu-icon {
        border: none;
        background-color: white;
        padding: 4px 0 0 0;
    }

    nav {
        position: fixed;
        top: 60px;
        transition: top 0.1s;
        z-index: 1;
        height: calc(100% - 74px); /*74px is height of the header*/
        background-color: var(--main-background-color);
        width: 100%;
    }
    .hidden {
        display: none;
    }
    ul {
        margin: 0;
        padding: 0;
    }
    li {
        display: flex;
        justify-content: center;
        padding-top: 10px;
        padding-bottom: 10px;
    }
    li:hover, li:active {
        background-color: var(--light-gray-color);
    }
    .active {
        color: black;
    }
    @media (min-width: 1155px) {
        .header {
            display: none;
        }
    }
</style>